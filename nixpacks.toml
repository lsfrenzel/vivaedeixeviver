# Railway/Nixpacks configuration for Python Flask app
providers = ["python"]

[phases.build]
cmds = ["flask db upgrade || true", "python init_db.py"]

[start]
cmd = "gunicorn --bind 0.0.0.0:$PORT --reuse-port main:app"
